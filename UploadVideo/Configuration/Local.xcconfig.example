// Copy to Local.xcconfig (gitignored). Use the Mac's LAN IP for physical iPhones.
API_BASE_URL = http:/$()/192.168.1.10:8080
// For TestFlight: use https and set the real signing team and bundle identifier.
// DEVELOPMENT_TEAM = YOUR_TEAM_ID
// PRODUCT_BUNDLE_IDENTIFIER = your.domain.UploadVideo
